{
  "name": "cv-ai-worker",
  "version": "0.0.1",
  "private": true,
  "description": "Cloudflare Worker that proxies questions to the Claude API for the CV's AI tab",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251101.0",
    "typescript": "~5.9.2",
    "wrangler": "^4.0.0"
  }
}
